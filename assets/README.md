Images here are for the **GitHub repository README** only. They are not loaded by the Chrome extension (the extension uses files under `icons/` and the rest of the extension tree).

Reference them from the repo root `README.md` with relative paths, for example:

`![Description](assets/your-image.png)`
